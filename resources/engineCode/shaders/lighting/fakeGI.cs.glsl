// +x means we run from xmax to xmin
// -x means we run from xmin to xmax
// +y means we run from ymax to ymin
// -y means we run from ymin to ymax
// +z means we run from zmax to zmin
// -z means we run from zmin to zmax
